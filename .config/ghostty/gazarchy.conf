# Gazarchy overrides on top of the Omarchy base ghostty config.

font-size = 10
font-style = Regular

shell-integration = none

keybind = f11=toggle_fullscreen
# Plain ESC+CR for Shift+Enter instead of the base's CSI-u encoding.
keybind = shift+enter=text:\x1b\r
